/*
 * To compile and inspect this program:
 *
 * 1. Compile the program using gcc:
 *    > gcc hello.c
 *
 * 2. View the compilation process and details:
 *    > gcc -v hello.c
 *
 * 3. Check the size of the generated output file:
 *    > ls -l a.out
 *
 * This will print "Hello, OS World!" to the terminal.
 */

#include <stdio.h>

int main()
{
    printf("Hello, OS World!\n");
}
